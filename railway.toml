[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# 비용 절감을 위해 유휴 시 슬립 허용
sleepModeEnabled = true
# 헬스체크 설정
healthcheckPath = "/health"
healthcheckTimeout = 300
